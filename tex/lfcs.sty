\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{lfcs}[2026/08/26 Shared paper configuration]

\RequirePackage{amsmath}
\RequirePackage{amsthm}
\RequirePackage{mathtools}
\RequirePackage{fontspec}
\RequirePackage{unicode-math}

\unimathsetup{
  math-style = ISO,
  bold-style = ISO,
  nabla = upright,
  partial = upright
}

\setmainfont{TeX Gyre Termes}
\setmathfont{TeX Gyre Termes Math}
\RequirePackage{microtype}


% Figures, tables, colour
\RequirePackage{graphicx}
\RequirePackage{xcolor}
\RequirePackage{booktabs}
\RequirePackage{tabularx}

% Bibliography
\RequirePackage[useprefix=true, style=apa, sortcites=false,
  sorting=nyt, uniquename=false, uniquelist=false, backend=biber,
  hyperref=true]{biblatex}
\addbibresource{bib/references.bib}

% Hyperlinks: load near the end

\RequirePackage[
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue
]{hyperref}
\RequirePackage[nameinlink,noabbrev]{cleveref}

% Colloboration (notes)
\usepackage[colorinlistoftodos,
prependcaption,
textsize=tiny,
backgroundcolor=orange!30,
textwidth=80pt]{todonotes}

% Linguistic examples
\RequirePackage{gb4e}
\RequirePackage{linguex}
  \renewcommand{\refdash}{}
  \setlength{\Exlabelsep}{0.5em}


\input{tex/metadata}
\input{tex/macros}

\endinput
